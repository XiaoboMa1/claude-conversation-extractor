## task 2: session name (fallback to session id)

### argument
current extract command is unhandy: You must type in session ID
`extract -e <id> -o` 
we need to support <session-name> as identifier, e.g.
- extract: 
`extract -e <seiion-name-specified-in-terminal> -o ...`
-  thinking disaplay:
`extract -t <seiion-name-specified-in-terminal>`

### output filename
- for default chat log file name (output), should be brief and useful, change to `claude-chat-<session-name>.md` (IF session name was not specified then Fall back to FIRST 6 digit / char of session ID ) 

first，you need to explain and verify:
- issue: In one session, User can change the session name by /rename， how do you match that?

## task 3: merge messages of same speaker + strip emmojis

### merge mesages
current extracted logs, Neighboring messages of the same speaker is segmented with ## headers, We should make the format more compact by Marging Neighboring messages of the same you speaker in the same turn  


#### current log
```
## 👤 User

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands... them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 👤 User

<command-name>/effort</command-name>
            <command-message>effort</command-message>
            <command-args>max</command-args>

---

## 👤 User

<local-command-stdout>Set effort...asoning (Opus 4.6 only)</local-command-stdout>

---

## 👤 User

 Based on above decisions, revise data scale and description one accordingly -  Do not present it as a corrected version. That is the final version for presentation. Do not expose your internal correction process.

---

## 🤖 Claude

You're right — I treated formatted text as empirical data. Let me re-derive from source code behavior and engineering fundamentals, then revise both files.

---

## 🤖 Claude

理解了。console output 是构造的，不能作为事实来源。

---

## 🤖 Claude

You've hit your limit · resets 1:20pm (Europe/London)

---

## 🤖 Claude

Prompt is too long

---

## 👤 User

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
```

#### after: cleaned log
```
## User

Based on above decisions, revise data scale and description one accordingly -  Do not present it as a corrected version. That is the final version for presentation. Do not expose your internal correction process.

---

## Claude

You're right — I treated formatted text as empirical data. Let me re-derive from source code behavior and engineering fundamentals, then revise both files.

理解了。console output 是构造的，不能作为事实来源。

---

## User

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
```

there is some noise that cannot be matched by tags:
- "You've hit your limit" and "Prompt is too long" : Try filter out by string match

#### filter out:
- <local-command-caveat> 
- <command-name>
- <local-command-stdout>
#### remove 
any emojis from D:\dev-assist\ai-tool\claude-extractor\src\extract_claude_logs.py
#### merge meesages in the same turn 

## task 4 - refactor code stucture
- D:\dev-assist\ai-tool\claude-extractor\src\extract_claude_logs.py
This file is too long, How to separate them into two or three files For better maintainability?
- code re-structure should never break any existing functions